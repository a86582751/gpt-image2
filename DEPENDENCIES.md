# 依赖说明

## Python 版本

建议使用 Python 3.10 或更高版本。

## 核心依赖

- `gradio`：构建本地 Web UI。
- `requests`：调用图片生成、文本模型和多模态模型接口。
- `Pillow`：压缩和转换上传给多模态模型的图片。

## 安装方式

```bash
pip install -r requirements.txt
```

## 可选建议

如果需要长时间批量生成，建议：

- 保持电脑不休眠。
- 使用稳定网络。
- 根据接口稳定性调整图片并发数量。
- 若代理软件影响长连接稳定性，可将相关接口域名设置为直连。
